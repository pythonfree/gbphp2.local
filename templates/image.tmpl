<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Работа с файлами</title>
    <link rel="stylesheet" href="css/style.css">
    <title>{{ title }}</title>
  </head>
  <body>
  <a href="{{ indexLink }}"> Вернуться в галерею </a>

  <div>
    <img src="{{ PHOTO }}{{ image }}">
  </div>

  </body>
</html>
